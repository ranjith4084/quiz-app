import 'package:firstflutter_app/quiz.dart';
import 'package:test/test.dart';

void main() {
  test('returns String and bool', () {
    final result = Quiz(
      answerQuestion: null,
      questionIndex: null,
      questions: [],
    );
    expect(String, String);
  });
}
